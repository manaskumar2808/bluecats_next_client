import { IconBaseProps } from "react-icons";
import { BsInstagram } from "react-icons/bs";

const InstagramIcon = (props: IconBaseProps) => {
    return <BsInstagram {...props} />
}

export default InstagramIcon;