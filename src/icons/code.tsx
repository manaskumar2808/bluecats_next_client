import { IconBaseProps, IconType } from 'react-icons';
import { BsCodeSquare } from 'react-icons/bs';

const CodeIcon: IconType = (props: IconBaseProps) => {
    return <BsCodeSquare {...props} />
}

export default CodeIcon;