import { IconBaseProps } from "react-icons";
import { BsPersonPlus } from "react-icons/bs";

const SignupIcon = (props: IconBaseProps) => {
    return <BsPersonPlus {...props} />
}

export default SignupIcon;