import { IconBaseProps, IconType } from "react-icons";
import { BsFacebook } from "react-icons/bs";

const FacebookIcon: IconType = (props: IconBaseProps) => {
    return <BsFacebook {...props} />
}

export default FacebookIcon;