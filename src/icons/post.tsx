import { IconBaseProps } from "react-icons";
import { BsPlusSquare } from 'react-icons/bs';

const PostIcon = (props: IconBaseProps) => {
    return <BsPlusSquare {...props} />
}

export default PostIcon;