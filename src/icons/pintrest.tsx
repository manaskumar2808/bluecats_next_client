import { IconBaseProps } from "react-icons";
import { BsPinterest } from "react-icons/bs";

const PintrestIcon = (props: IconBaseProps) => {
    return <BsPinterest {...props} />
}

export default PintrestIcon;