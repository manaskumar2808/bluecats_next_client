import { IconBaseProps } from "react-icons";
import { BsWhatsapp } from "react-icons/bs";

const WhatsappIcon = (props: IconBaseProps) => {
    return <BsWhatsapp {...props} />
}

export default WhatsappIcon;