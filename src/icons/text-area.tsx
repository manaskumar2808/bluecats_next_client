import { IconBaseProps, IconType } from 'react-icons';
import { BsTextareaT } from 'react-icons/bs';

const TextAreaIcon: IconType = (props: IconBaseProps) => {
    return <BsTextareaT {...props} />
}

export default TextAreaIcon;