import { IconBaseProps } from "react-icons";
import { BsPerson } from 'react-icons/bs';

const ProfileIcon = (props: IconBaseProps) => {
    return <BsPerson {...props} />    
}

export default ProfileIcon;