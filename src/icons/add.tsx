import { IconBaseProps, IconType } from 'react-icons';
import { BsPlusLg } from 'react-icons/bs';

const AddIcon: IconType = (props: IconBaseProps) => {
    return <BsPlusLg {...props} />
}

export default AddIcon;