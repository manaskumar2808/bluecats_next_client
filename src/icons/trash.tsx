import { IconBaseProps, IconType } from 'react-icons';
import { BsTrash } from 'react-icons/bs';

const TrashIcon: IconType = (props: IconBaseProps) => {
    return <BsTrash {...props} />
}

export default TrashIcon;